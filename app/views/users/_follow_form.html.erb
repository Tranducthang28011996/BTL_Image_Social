<% if current_user.following? user %>
  <%= link_to "Unfollow", unfollow_path(user), method: :delete, class: "btn-follow"  %>
<% else %>
  <%= link_to "follow", follow_path(user), method: :post, class: "btn-follow"  %>
<% end %>
