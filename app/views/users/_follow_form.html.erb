<% if current_user.following? user %>
  <%= link_to "following", unfollow_path(user), method: :delete, class: "btn-follow edit-profile"  %>
<% else %>
  <%= link_to "follow", follow_path(user), method: :post, class: "btn-follow edit-profile"  %>
<% end %>
